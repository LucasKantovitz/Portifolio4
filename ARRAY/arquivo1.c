#include <stdio.h>

int main (){
    int numeros[5];

    numeros [0] = 10;
    numeros [1] = 20;
    numeros [2] = 30;
    numeros [3] = 40;
    numeros [4] = 50;

    printf("Primeira posição:, %d\n", numeros[0]);
    printf("posição Central:, %d\n", numeros[2]);
    printf("ultima posição :, %d\n", numeros[4]);

    return 0;
}